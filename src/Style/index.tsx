import * as React from 'react';
import './style.scss';

export interface IStyleProps {
}

export default class Style extends React.Component<IStyleProps, any> {
    public render() {
        return (
            <div></div>
        );
    }
}